import 'package:flutter/material.dart';

void main() {
  runApp(null);
}

// TODO (1): Type "stl". This is the shortcut to create a stateless widget
// TODO (2): Name the stateless widget, "MyApp"
// TODO (3): Now replace the null on line 4 with MyApp(). This is because we want
//  to run our MyApp() widget when the app starts.